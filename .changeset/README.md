# Changesets

Changesets owns version bumps and changelog entries for the independently
released client-web Extension Host Runtime. Add a changeset to any pull request
that changes its published behavior.

Python package versions and changelogs are managed by Changie instead.
